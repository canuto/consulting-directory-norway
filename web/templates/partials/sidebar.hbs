<aside class="w-64 bg-gray-800 min-h-screen p-4">
    <a href="/" class="text-white text-xl font-bold mb-8 block">Directory Site</a>
    <nav>
        {{#each categories}}
        <a href="/category/{{slug}}" 
           class="block text-gray-300 hover:text-white py-2 {{#if active}}bg-gray-700{{/if}}">
            {{name}}
        </a>
        {{/each}}
    </nav>
</aside> 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{listing.title}} - Directory</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="flex">
        {{> sidebar}}
        
        <main class="flex-1 p-8">
            <div class="max-w-4xl mx-auto">
                <img src="{{listing.imageUrl}}" alt="{{listing.title}}" class="w-full h-96 object-cover rounded-lg shadow-lg">
                
                <div class="mt-8">
                    <h1 class="text-4xl font-bold mb-4">{{listing.title}}</h1>
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <div class="prose max-w-none">
                            {{{listing.content}}}
                        </div>
                        
                        <div class="mt-6 border-t pt-6">
                            <h2 class="text-2xl font-bold mb-4">Details</h2>
                            <dl class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                {{#each listing.details}}
                                <div>
                                    <dt class="font-semibold text-gray-600">{{label}}</dt>
                                    <dd>{{value}}</dd>
                                </div>
                                {{/each}}
                            </dl>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html> 
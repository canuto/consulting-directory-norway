{
  "name": "simple_web",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "clean": "coho dropcollection --collection 'listings'",
    "setup": "npm run clean && coho createcollection --collection 'listings' && coho import -f bin/output.json -c listings",
    "buildindex": "coho query listings > bin/database-listings.json && node bin/buildindex.js",
    "css": "npx @tailwindcss/cli -i ./web/css/input.css -o ./web/css/output.css",
    "build": "npm run css && npm run buildindex",
    "bundle": "esbuild ./clientscript.js --bundle --outfile=./web/js/bundle.js",
    "deploy": "npm run build && npm run bundle && codehooks deploy && codehooks logs --follow"
  },
  "dependencies": {
    "codehooks-js": "^1.2.20",
    "handlebars": "^4.7.8",
    "handlebars-layouts": "^3.1.4",
    "lunr": "^2.3.9"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.15",
    "esbuild": "^0.25.1",
    "tailwindcss": "^4.0.9"
  }
}
